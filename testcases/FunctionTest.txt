ORIGIN IS(300,300);
SCALE IS(20,20);

FOR T FROM 0 TO 1000 STEP 0.01 DRAW(T,SIN(T));

FOR T FROM 0 TO 1000 STEP 0.01 DRAW(T,COS(T));

FOR T FROM 0 TO 1000 STEP 0.01 DRAW(T,TAN(T));

FOR T FROM 0 TO 1000 STEP 0.01 DRAW(T,EXP(T));

FOR T FROM 0 TO 1000 STEP 0.01 DRAW(T,LN(T));

FOR T FROM 0 TO 1000 STEP 0.01 DRAW(T,SQRT(T));